// ON / OFF SWITCH
function on() {
  const element = document.getElementById("target");
  element.classList.add("on");
}
function off() {
  const element = document.getElementById("target");
  element.classList.remove("on");
}
